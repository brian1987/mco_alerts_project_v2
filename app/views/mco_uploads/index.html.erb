<h1>Listing mco_uploads</h1>
<div>
<!-- <%#= link_to "New Alert", new_alert_path, class: "btn btn-primary" %> -->
</div>
<table>
  <thead>
    <tr>
      <th>First name</th>
      <th>Last name</th>
      <th>Dob</th>
      <th>Medicaid</th>
      <th>Alert</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @mco_uploads.each do |mco_upload| %>
      <tr>
        <td><%= mco_upload.first_name %></td>
        <td><%= mco_upload.last_name %></td>
        <td><%= mco_upload.dob %></td>
        <td><%= mco_upload.medicaid_id %></td>
        <td><%= mco_upload.alert %></td>
        <td><%= link_to 'Show', mco_upload %></td>
        <td><%= link_to 'Edit', edit_mco_upload_path(mco_upload) %></td>
        <td><%= link_to 'Destroy', mco_upload, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Mco upload', new_mco_upload_path %>
